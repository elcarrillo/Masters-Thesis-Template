\documentclass[12pt,letterpaper]{report}

% Citations and References
\usepackage[round]{natbib}
\bibliographystyle{abbrvnat}

% Page Layout
\usepackage{geometry} % Customizing page layout

% Headers and Footers
\usepackage{fancyhdr} % Customizing headers and footers

% Captions
\usepackage[font=footnotesize,skip=2pt]{caption} % Customizing captions of figures and tables

% Mathematics
\usepackage{amsmath,amssymb,amsbsy} % Additional math symbols and formatting options

% Tables
\usepackage{dcolumn,array} % Column types for decimal-aligned tables
\usepackage{multirow} % Cells spanning multiple rows in tables
\usepackage{booktabs} % Professional table typesetting

% Units and Values
\usepackage{siunitx} % Typesetting units and values with proper spacing and formatting

% Floating Objects
\usepackage{stfloats} % Improved handling of floats (figures and tables) in two-column documents
\usepackage{float} % Improved placement of floating objects

% Images
\usepackage{graphicx} % Including and manipulating images in the document
\graphicspath{ {./images/} } % Directory for images

% Hyperlinks and Bookmarks
% \usepackage{hyperref} % Adding hyperlinks and bookmarks to the PDF

% Appendices
\usepackage[toc,page]{appendix} % Customizing appendices

% Additional Packages
\usepackage{layout} % Displaying the layout of the current page
\usepackage{relsize} % Changing the size of text relative to the current font size
\usepackage{psfrag} % Replacing text in EPS figures with LaTeX constructions
\usepackage{MnSymbol} % Additional math symbols
\usepackage{mathtools} % Enhanced typesetting of mathematical formulas
\usepackage{nomencl} % Creating nomenclatures or lists of symbols
\usepackage{bm} % Using bold symbols in math mode
\usepackage{chemformula} % Typesetting chemical formulas
\usepackage{afterpage} % Executing commands after the current page
\usepackage{adjustbox} % Adjusting the size of content
\usepackage{setspace} % Setting the spacing between lines


 \geometry{ %% set margin size to 1.5" on left and 1" for else
 left   = 38.1mm,
 top    = 25.4mm,
 bottom = 25.4mm, 
 right  = 25.4mm, 
 }

% \setlength{\headheight}{15pt}

% \DeclarePairedDelimiter\norm{\lVert}{\rVert}

% \makenomenclature

% \setlength{\headheight}{15pt} % Set the height of the header to 15 points

% \usepackage{mathtools} % Required for \DeclarePairedDelimiter command

% \DeclarePairedDelimiter{\norm}{\lVert}{\rVert} % Define a paired delimiter \norm for typesetting the norm of a vector or expression

% \usepackage{nomencl} % Required for creating nomenclatures or lists of symbols

% \makenomenclature % Enable the generation of a nomenclature


% \usepackage{algorithm}        
% \usepackage{algpseudocode}
